# 智慧医疗小程序项目总结终版

> **项目定位**：全流程互联网医疗服务平台  
> **覆盖范围**：微信/支付宝/抖音/鸿蒙多端  
> **核心数据**：9个业务模块 | 116个标准化接口 | 已上线项目服务真实患者  
> **技术栈**：Vue3 + TypeScript + UniApp + Pinia  

---

## 目录

1. [项目概述与核心价值](#一项目概述与核心价值)
2. [业务架构全景](#二业务架构全景)
3. [核心业务域全流程详解](#三核心业务域全流程详解)
4. [特色业务场景深度解析](#四特色业务场景深度解析)
5. [技术架构与选型决策](#五技术架构与选型决策)
6. [多端接入与配置体系](#六多端接入与配置体系)
7. [运营风控与质量保障](#七运营风控与质量保障)
8. [竞品分析与市场定位](#八竞品分析与市场定位)
9. [项目成果与价值验证](#九项目成果与价值验证)

---

## 一、项目概述与核心价值

### 1.1 项目背景

智慧医疗服务平台是一套综合性互联网医疗解决方案，通过与医院信息系统深度集成，为患者提供全场景、全周期的就医服务体验。

### 1.2 核心价值主张

```
┌─────────────────────────────────────────────────────────────┐
│                      患者价值主张                            │
├─────────────────────────────────────────────────────────────┤
│  ✓ 少跑腿：80%业务可在线办理                                │
│  ✓ 少等待：分时段预约，精确到分钟                           │
│  ✓ 少排队：移动支付，即付即走                               │
│  ✓ 更透明：费用、报告、病历随时可查                         │
│  ✓ 更便捷：手机即服务，随时随地办理                         │
│  ✓ 更安心：手术进度实时跟踪，住院费用透明                   │
└─────────────────────────────────────────────────────────────┘
```

### 1.3 服务覆盖范围

| 服务类型 | 覆盖场景 |
|----------|----------|
| **门诊服务** | 建档、预约、挂号、缴费、报告、病历 |
| **住院服务** | 入院、预交金、费用清单、手术、出院 |
| **便民服务** | 发票、物价、配送、开单、检查预约 |
| **诊前服务** | 导诊、预问诊、分诊 |

---

## 二、业务架构全景

### 2.1 系统架构拓扑

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         患者服务端（多端统一）                            │
├─────────────────────────────────────────────────────────────────────────┤
│  微信小程序  │  支付宝小程序  │  抖音小程序  │  鸿蒙元服务  │  Web/H5   │
├─────────────────────────────────────────────────────────────────────────┤
│                         统一网关层（API Gateway）                         │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐      │
│  │ 身份认证  │ │ 权限控制  │ │ 流量控制  │ │ 日志记录  │ │ 限流熔断  │      │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘ └──────────┘      │
├─────────────────────────────────────────────────────────────────────────┤
│                         业务中台（PHS核心能力）                           │
│  患者中心  │  挂号预约  │  支付结算  │  智慧药房  │  住院服务  │ 消息中心 │
├─────────────────────────────────────────────────────────────────────────┤
│                         接口适配层（HIS/医保集成）                        │
│  协议转换  │  数据映射  │  格式转换  │  错误处理  │  日志审计            │
├─────────────────────────────────────────────────────────────────────────┤
│                         外部系统层                                        │
│  HIS/EMR系统  │  医保结算平台  │  银联/第三方支付  │  药品SPD系统        │
└─────────────────────────────────────────────────────────────────────────┘
```

### 2.2 消息引擎数据流

**核心流程解析：**

```
数据接入层 → 消息处理层 → 推送执行层 → 监控反馈层
     │              │              │              │
     ▼              ▼              ▼              ▼
HIS/护理数据    订阅管理        模板组装       推送查询
检查预约数据    延迟管理        多渠道分发      数据统计
   ↓           优先级控制          ↓              ↓
标准化处理    场景匹配        微信/短信/APP   统计分析
```

### 2.3 接口能力矩阵（116个标准化接口）

| 功能域 | 接口数量 | 核心能力 |
|--------|----------|----------|
| 预约挂号 | 30 | 号源、排班、预约全流程 |
| 住院服务 | 18 | 住院、费用、手术、结算 |
| 门诊缴费 | 12 | 支付、医保、退费 |
| 患者信息 | 10 | 建档、认证、健康卡管理 |
| 报告查询 | 7 | 检验、检查、云影像 |
| 病历处方 | 7 | 病历、处方、出院小结 |
| 药品配送 | 8 | 地址、配送、物流 |
| 检查预约 | 5 | 检查预约、时段管理 |

---

## 三、核心业务域全流程详解

### 3.1 患者域：建档与身份治理

#### 患者身份生命周期

```
状态流转：

未建档 → 建档中 → 已绑定
  │         │         │
  │         ▼         │
  │      建档失败 ←───┘
  │                   │
  └────────────── 已建档未绑定
                      │
                      ▼
                  验证中 ──→ 人脸核身
                      │         │
                      └─────────┘
                            │
                            ▼
                        已绑定 ←──→ 更新中
                            │
                            ▼
                        解绑中 → 已解绑 → 归档
```

#### 建档流程产品细节

**业务规则矩阵：**

| 证件类型 | 适用人群 | 号码规则 | 特殊业务限制 | 监护人要求 |
|---------|---------|---------|-------------|-----------|
| **居民身份证** | 大陆居民 | 18位 | 无 | 14岁以下需监护人 |
| **护照** | 外籍人士 | 各国格式 | 暂不支持医保结算 | 无 |
| **港澳通行证** | 港澳居民 | 9位(H/M开头) | 需居住证才能医保 | 无 |
| **台湾通行证** | 台湾居民 | 8位数字 | 需居住证才能医保 | 无 |
| **出生医学证明** | 新生儿 | 7位字母数字 | 仅支持自费 | 必须监护人 |

**技术实现思路**：

```typescript
// 伪代码：患者建档核心流程
interface PatientRegistrationFlow {
  // 步骤1: 查询HIS是否存在档案
  async queryExistingPatient(idCard: string): Promise<PatientQueryResult> {
    const result = await hisApi.queryPatient({ idCard });
    
    if (result.exists) {
      // 已存在 → 进入绑定流程
      return this.enterBindingFlow(result.patientInfo);
    }
    // 不存在 → 进入建档流程
    return this.enterCreationFlow();
  }

  // 步骤2: 建档流程
  async createPatient(info: PatientInfo): Promise<Patient> {
    // 14岁以下强制监护人信息
    if (this.isMinor(info.birthDate)) {
      this.validateGuardianInfo(info.guardian);
    }
    
    // 证件格式校验
    this.validateIdCardFormat(info.idCardType, info.idCard);
    
    // 调用HIS建档接口
    const patient = await hisApi.createPatient(info);
    return patient;
  }

  // 步骤3: 绑定流程（含人脸核身判断）
  async bindPatient(userId: string, patientInfo: PatientInfo): Promise<BindingResult> {
    // 信息一致性校验
    const consistency = await this.checkInfoConsistency(patientInfo);
    
    if (!consistency.phoneMatch) {
      // 手机号不一致 → 触发人脸核身
      return this.triggerFaceVerification(patientInfo);
    }
    
    // 直接绑定
    return this.doBinding(userId, patientInfo.patientId);
  }
}

// 风控规则：72小时增删限制
class BindingFrequencyGuard {
  async checkLimit(phone: string): Promise<boolean> {
    const count = await this.getRecentOperationCount(phone, { 
      hours: 72, 
      operation: ['add', 'remove'] 
    });
    return count < 3; // 72小时内最多3次
  }
}
```

---

### 3.2 挂号预约域：号源管理与锁号机制

#### 号源库存模型

**核心业务概念：**
- **排班（Schedule）**：医生某天的出班计划
- **号源（Source）**：具体到时段的可预约名额
- **锁号（Lock）**：15分钟预占机制（防止并发超卖）

**号源状态流转：**

```
初始状态: 可用
  ↓ 用户点击预约
锁号中（15分钟）← Redis分布式锁
  ↓ 支付成功
已预约（确认号源）
  ↓ 就诊前1小时
待就诊（可在线取号）
  ↓ 患者取号/医生接诊
就诊中 → 已就诊

异常流：
- 锁号中 + 15分钟超时 → 自动释放（回滚库存）
- 锁号中 + 主动取消 → 立即释放
- 已预约 + 医生停诊 → 强制取消+退款
- 已预约 + 患者爽约 → 记违约（影响后续预约权限）
```

#### 锁号状态机

```typescript
// 伪代码：预约状态机
enum AppointmentStatus {
  LOCKED = 'locked',           // 锁号中（15分钟）
  PENDING_PAYMENT = 'pending', // 待支付
  CONFIRMED = 'confirmed',     // 已预约
  WAITING = 'waiting',         // 待就诊
  IN_PROGRESS = 'in_progress', // 就诊中
  COMPLETED = 'completed',     // 已就诊
  CANCELLED = 'cancelled',     // 已取消
  EXPIRED = 'expired',         // 已过期
  STOPPED = 'stopped'          // 已停诊
}

class AppointmentStateMachine {
  // 状态流转规则
  private transitions: Record<AppointmentStatus, AppointmentStatus[]> = {
    [AppointmentStatus.LOCKED]: [
      AppointmentStatus.PENDING_PAYMENT,
      AppointmentStatus.EXPIRED
    ],
    [AppointmentStatus.PENDING_PAYMENT]: [
      AppointmentStatus.CONFIRMED,
      AppointmentStatus.CANCELLED
    ],
    [AppointmentStatus.CONFIRMED]: [
      AppointmentStatus.WAITING,
      AppointmentStatus.CANCELLED,
      AppointmentStatus.STOPPED
    ],
    [AppointmentStatus.WAITING]: [
      AppointmentStatus.IN_PROGRESS,
      AppointmentStatus.EXPIRED
    ],
    [AppointmentStatus.IN_PROGRESS]: [AppointmentStatus.COMPLETED],
    // ... 其他状态
  };

  async transition(
    currentStatus: AppointmentStatus, 
    targetStatus: AppointmentStatus,
    context: TransitionContext
  ): Promise<boolean> {
    // 验证流转是否合法
    if (!this.transitions[currentStatus]?.includes(targetStatus)) {
      throw new InvalidTransitionError(`${currentStatus} -> ${targetStatus}`);
    }
    
    // 执行状态流转
    return this.executeTransition(targetStatus, context);
  }
}
```

#### 消息通知策略（场景化推送）

| 场景代码 | 触发时机 | 推送渠道 | 内容策略 | 优先级 |
|---------|---------|---------|---------|-------|
| **预约成功** | 支付回调 | 微信模板+短信 | 含就诊时间、地点、序号 | 高 |
| **就诊提醒** | 就诊前1小时 | 微信+APP Push | 导航链接+取号提示 | 高 |
| **临近叫号** | 队列前3位 | 微信+震动+语音 | 立即前往诊室 | **紧急** |
| **医生停诊** | 医院发起停诊 | 全渠道+短信 | 自动退款+改约链接 | **紧急** |
| **检查/取药提醒** | 医嘱开立 | 微信 | 地点导航+注意事项 | 中 |
| **危险值提醒** | 检验危急值 | 微信+短信+电话 | 立即就诊提示 | **紧急** |

---

### 3.3 门诊缴费域：医保与自费混合结算

#### 费用订单生命周期

```
医嘱开立 → 待缴费订单生成 → 患者查看 → 预结算 → 支付平台调起
                                              ↓
                                         医保分割计算
                                              ↓
                                    微信支付/支付宝
                                              ↓
                                    支付成功 ──→ HIS确认结算
                                    支付失败 ──→ 返回待缴费
                                              ↓
                                    已缴费 → 执行科室叫号

异常流：
- 24小时未支付 → 自动过期
- 退费申请 → 审核中 → 原路退款
```

#### 医保结算流程

**产品规则：**
1. **预结算**：支付前必须先调用医保接口进行费用分割，明确"统筹支付"与"个人自付"金额
2. **额度锁定**：医保部分需在医保平台锁定额度，防止支付过程中额度不足
3. **对账机制**：T+1日对账，差异订单自动冲正

```typescript
// 伪代码：医保结算流程
interface MedicalInsuranceSettlement {
  // 步骤1: 预结算 - 费用分割
  async preSettlement(orderId: string): Promise<PreSettlementResult> {
    const order = await this.getOrder(orderId);
    
    // 调用医保接口进行费用分割
    const splitResult = await medicalInsuranceApi.calculateSplit({
      patientId: order.patientId,
      totalAmount: order.totalAmount,
      items: order.items
    });
    
    return {
      totalAmount: order.totalAmount,
      insuranceCoverage: splitResult.coverage,    // 统筹支付
      personalPayment: splitResult.personal,      // 个人自付
      available: splitResult.insuranceAvailable   // 医保额度是否充足
    };
  }

  // 步骤2: 支付调起（自费部分）
  async initiatePayment(preSettlement: PreSettlementResult): Promise<PaymentIntent> {
    if (!preSettlement.available) {
      throw new InsuranceQuotaInsufficientError();
    }
    
    // 锁定医保额度
    await medicalInsuranceApi.lockQuota(preSettlement.insuranceCoverage);
    
    // 调起支付（仅个人自付部分）
    return paymentGateway.createPayment({
      amount: preSettlement.personalPayment,
      orderId: preSettlement.orderId
    });
  }

  // 步骤3: 支付回调确认
  async confirmSettlement(
    orderId: string, 
    paymentResult: PaymentResult
  ): Promise<SettlementResult> {
    if (paymentResult.success) {
      // 确认医保结算
      await medicalInsuranceApi.confirmSettlement(orderId);
      // 确认HIS结算
      await hisApi.confirmPayment(orderId);
      
      return { status: 'success', receiptNo: generateReceiptNo() };
    } else {
      // 释放医保额度
      await medicalInsuranceApi.releaseQuota(orderId);
      throw new PaymentFailedError();
    }
  }
}
```

---

### 3.4 智慧药房域：药品配送双入口策略

#### 业务场景矩阵（2×2×3）

| 入口\药品\配送 | 窗口自取 | 快递配送 | 仅填地址 |
|--------------|---------|---------|---------|
| **标准流程+自煎药** | ✅ 可选 | ❌ 不支持 | ✅ 仅留地址 |
| **标准流程+代煎药** | ✅ 可选 | ✅ 支持 | ✅ 仅留地址 |
| **扫码直达+自煎药** | ✅ 强制 | ❌ 不支持 | ❌ 不支持 |
| **扫码直达+代煎药** | ✅ 可选 | ✅ 支持 | ❌ 不支持 |

#### 配送策略路由引擎

**技术思路**：策略模式（Strategy Pattern）替代if-else嵌套

```typescript
// 伪代码：配送策略路由引擎

// 策略配置定义
interface DeliveryStrategy {
  id: string;
  allowedMethods: DeliveryMethod[];
  defaultMethod: DeliveryMethod;
  requireAddress: boolean;
  validate: (context: DeliveryContext) => ValidationResult;
}

// 策略配置表（12种组合）
const DELIVERY_STRATEGIES: Record<string, DeliveryStrategy> = {
  // 标准流程 + 自煎药
  'standard-self': {
    id: 'standard-self',
    allowedMethods: ['window', 'address_only'],
    defaultMethod: 'window',
    requireAddress: (method) => method === 'address_only',
    validate: (ctx) => ({ valid: true })
  },
  
  // 标准流程 + 代煎药
  'standard-proxy': {
    id: 'standard-proxy',
    allowedMethods: ['window', 'express', 'address_only'],
    defaultMethod: 'window',
    requireAddress: (method) => ['express', 'address_only'].includes(method),
    validate: (ctx) => {
      // 代煎药不能仅填地址（必须实际交付）
      if (ctx.selectedMethod === 'address_only') {
        return { valid: false, message: '代煎药品请选择窗口自取或快递配送' };
      }
      return { valid: true };
    }
  },
  
  // 扫码直达 + 自煎药
  'scan-self': {
    id: 'scan-self',
    allowedMethods: ['window'],
    defaultMethod: 'window',
    requireAddress: false,
    validate: (ctx) => ({ valid: true })
  },
  
  // 扫码直达 + 代煎药
  'scan-proxy': {
    id: 'scan-proxy',
    allowedMethods: ['window', 'express'],
    defaultMethod: 'window',
    requireAddress: (method) => method === 'express',
    validate: (ctx) => ({ valid: true })
  }
};

// 策略路由引擎
class DeliveryStrategyRouter {
  resolveStrategy(context: DeliveryContext): DeliveryStrategy {
    // 根据入口类型和煎药类型匹配策略
    const key = `${context.entryType}-${context.decoctionType}`;
    const strategy = DELIVERY_STRATEGIES[key];
    
    if (!strategy) {
      throw new StrategyNotFoundError(key);
    }
    
    return strategy;
  }
  
  getAvailableOptions(context: DeliveryContext): DeliveryOptions {
    const strategy = this.resolveStrategy(context);
    
    return {
      methods: strategy.allowedMethods,
      defaultMethod: strategy.defaultMethod,
      requireAddress: strategy.requireAddress(context.selectedMethod)
    };
  }
}

// 使用示例
const router = new DeliveryStrategyRouter();
const options = router.getAvailableOptions({
  entryType: 'standard',      // 标准流程入口
  decoctionType: 'proxy',     // 代煎药
  selectedMethod: 'express'   // 用户选择快递
});
// 返回: { methods: ['window', 'express', 'address_only'], defaultMethod: 'window', requireAddress: true }
```

#### 中西药混选拦截规则

**业务约束**（温州中西医个性化需求）：
1. **跨院区拦截**：不同院区药品不能一起配送（库存独立）
2. **中西药混选拦截**：西药与中药库存扣减逻辑不同，需分开下单
3. **代煎/自煎混选拦截**：煎煮方式冲突，不能合并

```typescript
// 伪代码：药品选择校验器
class DrugSelectionValidator {
  validate(cart: CartItem[]): ValidationResult {
    const errors: string[] = [];
    
    // 规则1: 跨院区检查
    const hospitals = new Set(cart.map(item => item.hospitalId));
    if (hospitals.size > 1) {
      errors.push('不同院区药品不能一起配送');
    }
    
    // 规则2: 中西药混选检查
    const hasWestern = cart.some(item => item.drugType === 'western');
    const hasChinese = cart.some(item => item.drugType === 'chinese');
    if (hasWestern && hasChinese) {
      errors.push('西药与中药不能一起下单');
    }
    
    // 规则3: 煎煮方式冲突检查
    const decoctionTypes = new Set(cart.map(item => item.decoctionType));
    if (decoctionTypes.has('self') && decoctionTypes.has('proxy')) {
      errors.push('代煎药品与自煎药品不能一起下单');
    }
    
    return {
      valid: errors.length === 0,
      errors
    };
  }
}
```

---

### 3.5 住院服务域：押金与日清单管理

#### 住院费用业务模型

| 概念 | 业务定义 | 产品展示 |
|-----|---------|---------|
| **押金（预交金）** | 入院时预缴，用于抵扣住院费用 | 显示余额、已缴金额、建议补缴 |
| **日清单** | 每日0点生成的昨日费用明细 | 按日期筛选，分类汇总 |
| **总费用** | 截至当前累计产生的费用 | 实时计算：已结+在院未结 |
| **预估费用** | 根据治疗方案预测的总费用 | 用于押金预警（余额<预估×30%） |

**欠费预警策略：**
- **余额充足**：绿色，无需操作
- **余额不足（<预估30%）**：黄色，建议充值
- **已欠费（余额<0）**：红色，立即充值（影响取药、检查预约）

---

## 四、特色业务场景深度解析

### 4.1 DRG医保结算与病案管理

#### DRG费用预警体系

**核心价值**：在诊疗过程中实时提示医生/患者费用消耗情况，避免超支。

| 费用消耗率 | 预警级别 | 颜色标识 | 干预策略 |
|-----------|---------|---------|---------|
| <80% | 安全 | 🟢 绿色 | 正常诊疗 |
| 80%-100% | 警告 | 🟡 黄色 | 提示关注，避免不必要检查 |
| 100%-200% | 危险 | 🔴 红色 | 启动特病单议申请流程 |
| >200% | 高倍率 | ⚫ 黑色 | 必须提交特病单议，否则亏损 |

#### 飞检风险防控

| 风险类型 | 识别逻辑 | 防控措施 |
|---------|---------|---------|
| **高倍率病例** | 费用>DRG标准200% | 强制启动特病单议流程 |
| **诊断不匹配** | 主要诊断与费用结构偏差>30% | 提醒医生核对诊断编码 |
| **药品异常** | 使用与诊断无关的高价药 | 提示药师审核，必要时拦截 |
| **重复检查** | 7天内相同检查项目 | 自动提示引用历史报告 |

---

### 4.2 MDT多学科会诊问诊

#### 预问诊业务流程

```
患者进入MDT问诊
  ↓
选择问诊类型: [初诊, 复诊]
  ↓
AI医生提问（单选/多选/文字/图片）
  ↓
患者回答 → 实时生成报告草稿
  ↓
补充主诉、上传历史资料
  ↓
生成预问诊报告: {主诉, 现病史, 初步诊断建议, 推荐科室}
  ↓
医生端查看报告 → 确认或修改 → 正式接诊
```

---

### 4.3 用药管理（用药提醒）

#### 用药提醒业务模型

**数据来源双通道：**
1. **本院处方导入**：自动同步医院开立的处方，一键生成提醒
2. **自定义添加**：用户手动输入药品信息（院外购药、保健品等）

**提醒策略：**

```typescript
// 伪代码：用药提醒引擎
interface MedicationReminder {
  // 药品维度
  drugInfo: {
    name: string;
    usage: 'oral' | 'external' | 'injection';
    frequency: 'once_daily' | 'three_times_daily' | 'alternate_day' | 'prn';
    dosage: string; // "每次2片"
  };
  
  // 时间维度
  schedule: {
    startDate: Date;
    endDate: Date;
    reminderTimes: string[]; // ["08:00", "12:00", "18:00"]
    advanceMinutes: number;  // 提前提醒分钟数
  };
  
  // 执行状态
  status: 'pending' | 'reminded' | 'confirmed' | 'skipped' | 'closed';
}

class MedicationReminderEngine {
  // 生成提醒计划
  generateReminders(medication: Medication): ReminderSchedule[] {
    const reminders: ReminderSchedule[] = [];
    const currentDate = new Date(medication.schedule.startDate);
    
    while (currentDate <= medication.schedule.endDate) {
      for (const time of medication.schedule.reminderTimes) {
        reminders.push({
          date: new Date(currentDate),
          time: time,
          status: 'pending',
          medicationId: medication.id
        });
      }
      // 根据频次递增日期
      currentDate.setDate(currentDate.getDate() + this.getIntervalDays(medication.frequency));
    }
    
    return reminders;
  }
}
```

---

### 4.4 住院管家（入院全流程）

#### 入院状态流转

```
状态1: 住院登记（已申请未入院）
  ├─ 展示: 申请科室、预计入院时间
  └─ 操作: 取消申请、修改期望时间

状态2: 检查/评估（院前准备）
  ├─ 展示: 院前检查项目列表（血常规、核酸等）
  ├─ 操作: 在线预约检查时间、查看检查结果
  └─ 自动流转: 所有检查完成后 → 自动进入等待床位

状态3: 等待床位（排队中）
  ├─ 展示: 当前排队序号、预计等待天数
  ├─ 通知: 床位即将就绪推送（前3位）
  └─ 操作: 预缴押金（提升优先级）、取消等待

状态4: 到院入科（正式入院）
  ├─ 展示: 床位号、管床医生、入院须知
  └─ 操作: 签署电子知情同意书、查看每日清单
```

---

## 五、技术架构与选型决策

### 5.1 技术栈总览

| 层级 | 技术组件 | 选型原因 |
|------|----------|----------|
| **前端框架** | Vue3 + Composition API | 更好的TypeScript支持，逻辑复用更方便 |
| **类型系统** | TypeScript 严格模式 | 100%类型覆盖，提前发现潜在错误 |
| **跨端方案** | UniApp | 一套代码支持小程序/H5/APP，降低维护成本 |
| **状态管理** | Pinia | Vue3官方推荐，类型友好，开发体验好 |
| **构建工具** | Vite | 快速的HMR，优化开发体验 |
| **HTTP客户端** | Axios | 成熟的拦截器机制，统一错误处理 |

### 5.2 架构设计原则

#### 分层架构设计

```
modules/
├── types/           # 类型定义层（人工设计，AI生成）
│   └── 职责: 定义领域模型、接口契约
├── store/           # 状态管理层（人工设计核心逻辑）
│   └── 职责: 全局状态、跨组件通信
├── api/             # API接口层（AI生成样板）
│   └── 职责: HTTP请求封装、接口调用
├── composables/     # 业务逻辑层（人工设计，AI辅助）
│   └── 职责: 可复用业务逻辑、状态管理
├── components/      # 组件层（AI生成基础，人工优化）
│   └── 职责: UI展示、用户交互
├── utils/           # 工具函数层（AI生成常用函数）
│   └── 职责: 通用工具、格式化函数
└── pages/           # 页面层（AI生成基础结构）
    └── 职责: 页面组装、路由配置
```

#### 单一职责原则示例

```typescript
// ❌ 不好的做法：所有逻辑混在一起
const useHospitalModule = () => {
  // 状态管理 + 数据获取 + UI交互 + 数据处理
};

// ✅ 好的做法：职责分离
// useHospitalStatus.ts - 只负责状态管理
export const useHospitalStatus = () => {
  const status = ref<HospitalStatus>('pending');
  const updateStatus = (newStatus: HospitalStatus) => {
    status.value = newStatus;
  };
  return { status, updateStatus };
};

// useHospitalData.ts - 只负责数据获取
export const useHospitalData = () => {
  const fetchHospitalInfo = async (patientId: string) => {
    return await api.getHospitalInfo({ patientId });
  };
  return { fetchHospitalInfo };
};

// useGuideList.ts - 只负责检查列表逻辑
export const useGuideList = () => {
  const { data, loading, execute } = useLoading();
  const loadGuideList = async () => {
    await execute(() => api.getGuideList());
  };
  return { guideList: data, loading, loadGuideList };
};

// 页面组件中组合使用
const HospitalPage = () => {
  const { status } = useHospitalStatus();
  const { fetchHospitalInfo } = useHospitalData();
  const { guideList, loadGuideList } = useGuideList();
  
  onMounted(() => {
    fetchHospitalInfo();
    loadGuideList();
  });
};
```

### 5.3 关键技术决策

#### 决策1：策略模式替代if-else嵌套

**问题背景**：智慧药房配送系统有2种入口 × 2种药品类型 × 3种配送方式 = 12种业务规则组合，if-else嵌套会导致代码难以维护。

**解决方案**：

```typescript
// 策略模式核心实现
interface DeliveryStrategy {
  match(context: DeliveryContext): boolean;
  getOptions(): DeliveryOptions;
  validate(context: DeliveryContext): ValidationResult;
}

// 具体策略实现
class StandardProxyStrategy implements DeliveryStrategy {
  match(ctx: DeliveryContext): boolean {
    return ctx.entryType === 'standard' && ctx.decoctionType === 'proxy';
  }
  
  getOptions(): DeliveryOptions {
    return {
      methods: ['window', 'express', 'address_only'],
      defaultMethod: 'window'
    };
  }
  
  validate(ctx: DeliveryContext): ValidationResult {
    // 代煎药不能仅填地址
    if (ctx.selectedMethod === 'address_only') {
      return { valid: false, message: '代煎药品请选择窗口或快递' };
    }
    return { valid: true };
  }
}

// 策略注册与路由
class StrategyRegistry {
  private strategies: DeliveryStrategy[] = [];
  
  register(strategy: DeliveryStrategy) {
    this.strategies.push(strategy);
  }
  
  resolve(context: DeliveryContext): DeliveryStrategy {
    return this.strategies.find(s => s.match(context)) 
      ?? throw new StrategyNotFoundError();
  }
}
```

**收益**：
- 第三次迭代新增冷链配送，仅新增2个策略文件（20行代码），半天完成开发测试
- 若用if-else预计需2天，且易引入bug

#### 决策2：配置驱动开发

**问题背景**：医院差异化需求多，硬编码修改维护成本高。

**解决方案**：

```typescript
// 医院配置 schema
interface HospitalConfig {
  // 业务开关
  features: {
    enableInsurance: boolean;      // 是否支持医保
    enableDelivery: boolean;       // 是否支持配送
    enableMDT: boolean;            // 是否支持MDT
  };
  
  // 界面配置
  theme: {
    primaryColor: string;          // 主题色
    logo: string;                  // 医院logo
    name: string;                  // 医院名称
  };
  
  // 业务规则配置
  rules: {
    appointment: {
      lockDuration: number;        // 锁号时长（分钟）
      maxBookingDays: number;      // 最大预约天数
      cancelBeforeHours: number;   // 提前取消小时数
    };
    binding: {
      maxPatients: number;         // 最大绑定就诊人数
      operationLimitHours: number; // 操作限制时间窗口
      operationLimitCount: number; // 操作限制次数
    };
  };
}

// 配置加载器
class ConfigManager {
  private config: HospitalConfig;
  
  async loadConfig(hospitalId: string): Promise<HospitalConfig> {
    // 优先从本地缓存加载
    const cached = uni.getStorageSync(`config_${hospitalId}`);
    if (cached && !this.isExpired(cached)) {
      return cached;
    }
    
    // 从服务器加载
    const config = await api.getHospitalConfig(hospitalId);
    uni.setStorageSync(`config_${hospitalId}`, config);
    return config;
  }
}
```

#### 决策3：统一错误处理机制

```typescript
// 错误分类体系
enum ErrorCategory {
  NETWORK = 'network',           // 网络错误
  BUSINESS = 'business',         // 业务错误
  VALIDATION = 'validation',     // 校验错误
  SYSTEM = 'system'              // 系统错误
}

// 统一错误处理器
class ErrorHandler {
  handle(error: ApiError): void {
    switch (error.category) {
      case ErrorCategory.NETWORK:
        this.handleNetworkError(error);
        break;
      case ErrorCategory.BUSINESS:
        this.handleBusinessError(error);
        break;
      case ErrorCategory.VALIDATION:
        this.handleValidationError(error);
        break;
      default:
        this.handleSystemError(error);
    }
  }
  
  private handleBusinessError(error: ApiError) {
    // 业务错误：toast提示用户
    uni.showToast({
      title: error.message,
      icon: 'none'
    });
    
    // 特定错误码处理
    if (error.code === 'APPOINTMENT_LOCKED') {
      // 号源被锁定，提示用户选择其他时段
      this.showAlternativeTimeSlots();
    }
  }
  
  private handleNetworkError(error: ApiError) {
    // 网络错误：提示检查网络，提供重试
    uni.showModal({
      title: '网络异常',
      content: '请检查网络连接后重试',
      showCancel: false,
      confirmText: '重试',
      success: () => error.retry?.()
    });
  }
}

// Axios拦截器集成
api.interceptors.response.use(
  response => response,
  error => {
    const apiError = this.transformError(error);
    errorHandler.handle(apiError);
    return Promise.reject(apiError);
  }
);
```

### 5.4 设计模式应用

| 设计模式 | 应用场景 | 解决的问题 |
|---------|---------|-----------|
| **策略模式** | 智慧药房配送规则 | 避免if-else嵌套，支持灵活扩展 |
| **适配器模式** | HIS接口适配 | 统一不同版本接口的调用方式 |
| **状态机模式** | 预约状态流转 | 清晰管理复杂状态转换 |
| **观察者模式** | 消息通知推送 | 解耦消息生产与消费 |
| **工厂模式** | 问卷组件创建 | 根据类型动态创建组件 |

---

## 六、多端接入与配置体系

### 6.1 平台差异化策略

| 平台 | 核心优势 | 特色功能 | 适配策略 |
|-----|---------|---------|---------|
| **微信小程序** | 生态完善，用户基数大 | 微信一键登录、亲属卡支付 | 主渠道，全功能覆盖 |
| **支付宝小程序** | 金融+医保能力强 | 芝麻信用免押、医保电子凭证 | 重点优化支付与医保 |
| **抖音小程序** | 内容种草，年轻用户 | 短视频健康科普 | 简化流程，重内容轻功能 |
| **鸿蒙元服务** | 系统级入口，IoT协同 | 服务卡片、分布式流转 | 原子化服务，免安装 |

### 6.2 老用户迁移策略（OpenID同步）

**业务背景**：医院切换小程序供应商，需将老系统用户无感知迁移到新系统。

```typescript
// 伪代码：OpenID同步流程
class UserMigrationService {
  async migrateUser(openId: string): Promise<MigrationResult> {
    try {
      // 1. 查询老系统档案
      const legacyProfile = await legacyApi.queryUserProfile(openId);
      
      if (!legacyProfile) {
        return { status: 'new_user', message: '无历史档案，走新用户流程' };
      }
      
      // 2. 数据同步（带重试机制）
      await this.syncWithRetry({
        patientProfiles: legacyProfile.patients,
        addresses: legacyProfile.addresses,
        bindings: legacyProfile.bindings
      });
      
      // 3. 标记已同步
      await this.markAsMigrated(openId);
      
      return { 
        status: 'success', 
        data: legacyProfile,
        message: '无感知迁移成功' 
      };
      
    } catch (error) {
      // 降级处理：允许用户手动填写
      return { 
        status: 'manual_required', 
        message: '同步失败，请手动填写信息' 
      };
    }
  }
  
  private async syncWithRetry(data: UserData, maxRetries = 3): Promise<void> {
    for (let i = 0; i < maxRetries; i++) {
      try {
        await this.syncData(data);
        return;
      } catch (error) {
        if (i === maxRetries - 1) throw error;
        await this.delay(1000 * Math.pow(2, i)); // 指数退避
      }
    }
  }
}
```

**成果指标**：迁移成功率100%，用户零感知，数据零丢失。

---

## 七、运营风控与质量保障

### 7.1 72小时增删限制（防黄牛）

**业务规则**：同一手机号72小时内最多增删就诊人3次。

```typescript
// 风控实现
class BindingFrequencyGuard {
  async checkLimit(phone: string): Promise<LimitCheckResult> {
    // 查询近72小时操作记录
    const operations = await this.operationLog.query({
      phone,
      operationTypes: ['add_patient', 'remove_patient'],
      since: new Date(Date.now() - 72 * 60 * 60 * 1000)
    });
    
    const count = operations.length;
    const remaining = Math.max(0, 3 - count);
    
    if (count >= 3) {
      const oldestOperation = operations[0];
      const unlockTime = new Date(oldestOperation.timestamp.getTime() + 72 * 60 * 60 * 1000);
      
      return {
        allowed: false,
        message: `操作频繁，请在 ${formatTime(unlockTime)} 后重试`,
        nextAvailableTime: unlockTime
      };
    }
    
    return {
      allowed: true,
      remaining,
      message: `剩余可添加次数：${remaining}`
    };
  }
}
```

### 7.2 客服知识库（18类问题）

**TOP 4高频问题及话术**：

| 问题 | 用户原话 | 问题根因 | 标准话术 | 技术优化 |
|-----|---------|---------|---------|---------|
| **检查预约** | "我想预约CT，怎么操作？" | 混淆"预约挂号"与"预约检查" | "检查需先开单：1.在线问诊开单 2.门诊挂号开单。开单后在此预约时间。" | 首页增加"先开单"提示页 |
| **当天号取消** | "今天有事来不了，怎么取消？" | 小程序限制当天号不可取消 | "当天号需到医院窗口取消，线上无法操作。" | 当天号按钮置灰+提示 |
| **无验证码** | "收不到验证码" | 触发72小时风控或运营商延迟 | "因安全考虑，72小时内限3次操作，请在XX时间后重试。" | 前端增加倒计时和频次提示 |
| **改手机号** | "我换手机号了，怎么改？" | 混淆登录手机号与就诊人手机号 | "登录号：我的→设置→账号安全；就诊人号：家庭成员→修改信息（需人脸验证）" | 页面增加区分提示 |

### 7.3 故障分级与响应

| 等级 | 定义 | 响应时间 | 处理时限 | 示例 |
|-----|------|---------|---------|------|
| **P0** | 核心功能不可用，>100用户影响 | 5分钟 | 2小时 | 支付中断、挂号失败 |
| **P1** | 主要功能异常，>10用户影响 | 15分钟 | 4小时 | 报告查询失败、消息未推送 |
| **P2** | 次要功能异常，<10用户影响 | 30分钟 | 8小时 | 页面样式错乱 |
| **P3** | 体验问题，无实质影响 | 2小时 | 24小时 | 文案错误 |

---

## 八、竞品分析与市场定位

### 8.1 行业分层定位

```
                        功能完整性
                             ↑
    卫宁/创业慧康            │        微医
    （传统HIS厂商）          │      （平台型）
                             │
    高投入 ←─────────────────┼─────────────→ 高流量
    深度定制                  │              平台抽成
                             │
         本产品 ←───────────┼───────→ 轻量级SaaS
      （医院共建型）          │       （标准化SaaS）
                             │
    平衡性价比                ↓           快速上线
                        实施周期/成本
```

### 8.2 核心能力对比矩阵

| 功能模块 | 本产品 | 微医 | 卫宁/创业 | SaaS平台 |
|----------|--------|------|-----------|----------|
| **挂号预约** | ✅✅✅ | ✅✅✅ | ✅✅✅ | ✅✅ |
| **预问诊/智能导诊** | ✅✅✅ | ✅✅ | ✅ | ✅ |
| **用药提醒** | ✅✅✅ | ✅✅ | ✅ | ❌ |
| **智能陪诊** | ✅✅✅ | ✅ | ✅✅ | ❌ |
| **住院服务** | ✅✅✅ | ✅ | ✅✅✅ | ❌ |
| **问卷随访** | ✅✅✅ | ✅✅ | ✅✅ | ✅ |
| **主题定制** | ✅✅✅ | ✅ | ✅ | ✅ |

### 8.3 成本优势分析

| 方案类型 | 价格区间 | 代表厂商 |
|---------|---------|---------|
| **平台型** | 按年服务费+交易抽成 | 微医、阿里健康 |
| **传统HIS厂商** | 1000万-5000万+ | 卫宁、创业慧康 |
| **本产品** | 百万级 | - |
| **轻量级SaaS** | 年费10-50万 | 各类SaaS厂商 |

**核心优势**：
- 比平台型产品省20%+，且无交易抽成
- 比传统HIS厂商省50%，同时保持较好的定制能力
- 比SaaS平台功能完整度高，支持深度定制

---

## 九、项目成果与价值验证

### 9.1 核心产出数据

| 指标 | 数据 | 说明 |
|------|------|------|
| **开发周期** | 3个月 | 传统方式预计需6个月+ |
| **业务模块** | 9个完整模块 | 8个重构模块 + 1个DRG模块自主设计 |
| **代码规模** | 2万+行 | 重构+DRG模块 |
| **代码精简率** | -62% | 从3139行→1190行 |
| **类型覆盖率** | 100% | TypeScript严格模式 |

### 9.2 上线项目成果

**智慧药房配送系统（已上线）：**

| 指标 | 结果 |
|------|------|
| 严重故障 | 0起 |
| 线上小问题 | 3起（均已修复） |
| 老用户迁移成功率 | 100% |
| 窗口排队患者减少 | 40%（医院信息科反馈） |
| "选错配送方式"投诉 | 降为0 |

### 9.3 业务价值验证

| 指标 | 效果 |
|------|------|
| 窗口排队时间 | 减少60%+ |
| 患者满意度 | 提升30%+ |
| 号源利用率 | 提升25%+ |
| 平均就医时间 | 缩短40%+ |
| 在线业务占比 | 超过70% |

---

## 附录

### A. 核心资料索引

| 资料名称 | 核心价值 | 建议精读章节 |
|---------|---------|-------------|
| 消息引擎平台接口文档 | 加密规则/场景代码 | 第2章接口规范、第4章数据字典 |
| PHS-HIS接口详细学习资料 | 116个接口业务逻辑 | 预约挂号模块(30个)、住院服务(18个) |
| 项目上线切换计划表 | 项目管理方法论 | 任务依赖关系、风险点标注 |

### B. 技术术语表

| 术语 | 说明 |
|------|------|
| **PHS** | Patient Health Service，患者健康服务中台 |
| **HIS** | Hospital Information System，医院信息系统 |
| **DRG** | Diagnosis Related Groups，疾病诊断相关分组 |
| **MDT** | Multi-Disciplinary Treatment，多学科会诊 |
| **CMI** | Case Mix Index，病例组合指数 |

### C. 核心接口清单

| 业务环节 | PHS接口 | HIS接口 | 关键字段 |
|---------|---------|--------|---------|
| 建档查询 | queryPatient | OpenPatientRes | patientId |
| 锁号 | lockAppointment | OpenRegisterRes | lockId(15分钟有效) |
| 确认挂号 | confirmAppointment | OpenRegisterRes | appointNo(就诊流水号) |
| 费用查询 | getOutpatientFeeList | 102101 | receiptNo(收据号) |
| 医保结算 | medicalCostInfoUpload | OpenSettleResou | payType: 3医保 |

---

**文档版本**: V1.0  
**更新日期**: 2026年3月  
**编制说明**: 本文档整合项目总结1、项目总结2及所有业务产出，形成全流程业务产品文档，技术方面聚焦选型决策与伪代码说明。

